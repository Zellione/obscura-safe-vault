#pragma once

// Shared helpers for the Qt experiment's headless tests: a minimal valid JPEG
// and test-speed vault construction (same KdfParams as tests/vault/test_vault.cpp).
// Test vaults + fixed test password only — never used outside test binaries.

#include <cstdint>
#include <cstdio>
#include <filesystem>
#include <span>
#include <stdexcept>
#include <string>
#include <vector>

#include "vault/vault.h"
#include "crypto/kdf.h"

namespace osvqt_test {

// Minimal valid JPEG (1x1 white pixel)
inline constexpr uint8_t tiny_jpeg[] = {
    
};

// Create (overwriting) a test vault at vault_path with password "test123" and
// test-speed KdfParams. The vault is unlocked on return. Throws on failure.
inline vault::Vault createTestVault(const std::string& vault_path)
{
    if (std::filesystem::exists(vault_path)) {
        std::filesystem::remove(vault_path);
    }

    std::string password = "test123";
    const std::span<const uint8_t> pw_span(
        reinterpret_cast<const uint8_t*>(password.data()), password.size());
    const std::span<const uint8_t> keyfile_span{};

    vault::Vault vault;
    crypto::KdfParams kdf_params{.t_cost = 1, .m_cost_kib = 8, .parallelism = 1};
    auto result = vault::Vault::create(vault_path, pw_span, keyfile_span, kdf_params, vault);
    if (result != vault::VaultResult::Ok) {
        fprintf(stderr, "Failed to create test vault\n");
        throw std::runtime_error("create vault failed");
    }

    // Vault is already unlocked after create
    return vault;
}

// Add `count` copies of tiny_jpeg to the root gallery, named <prefix><1..count>.
// progressEvery > 0 prints a progress line every that-many images. Throws on failure.
inline void addTinyImages(vault::Vault& vault, const char* prefix, int count, int progressEvery = 0)
{
    for (int i = 0; i < count; ++i) {
        std::string img_name = std::string(prefix) + std::to_string(i + 1);
        std::vector<uint8_t> image_data(tiny_jpeg, tiny_jpeg + sizeof(tiny_jpeg));
        const std::span<const uint8_t> img_span(image_data.data(), image_data.size());
        auto result = vault.add_image("", img_span, img_name);  // "" = root gallery
        if (result != vault::VaultResult::Ok) {
            fprintf(stderr, "Failed to add image %d\n", i);
            throw std::runtime_error("add_image failed");
        }
        if (progressEvery > 0 && (i + 1) % progressEvery == 0) {
            printf("  Added %d images\n", i + 1);
        }
    }
}

// createTestVault() plus one sub-gallery named "subfolder" and two images —
// the vault shape the gallery/detail tests need. Lives here rather than being
// copy-pasted per test file (which is what SonarCloud's duplication gate
// flagged). Throws on failure, like the helpers above.
inline vault::Vault createTestVaultWithSubgallery(const std::string& vault_path)
{
    vault::Vault vault = createTestVault(vault_path);

    auto result = vault.create_gallery("subfolder");
    if (result != vault::VaultResult::Ok) {
        fprintf(stderr, "Failed to create gallery\n");
        throw std::runtime_error("create_gallery failed");
    }

    addTinyImages(vault, "image", 2);
    return vault;
}

}  // namespace osvqt_test
