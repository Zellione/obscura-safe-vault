# Obscura-Safe-Vault — ROADMAP

> **Legend**
> - ✅ Done   🔜 In progress / planned   ⬜ Not started
> - Each phase ends with a clear acceptance criterion that must pass before work on the next phase begins.
> - Full spec for each phase (goal, tasks, acceptance criterion, status, follow-ups) lives in its own file under `docs/roadmap/`. This index is a scannable summary — open the linked file when working on that phase.

---

## Phase index

| # | Name | Status | Summary |
|---|---|---|---|
| 0 | Skeleton & minimal window | ✅ | Project structure, build system, a compilable app that opens a window. → [details](docs/roadmap/phase-00-skeleton.md) |
| 1 | Crypto core | ✅ | Full cryptographic primitive layer, implemented and tested. → [details](docs/roadmap/phase-01-crypto-core.md) |
| 2 | Vault container | ✅ | The `.osv` file format: header, index tree, chunk store, core vault API. → [details](docs/roadmap/phase-02-vault-container.md) |
| 3 | Image decode & thumbnails | ✅ | Decode images from decrypted memory buffers, generate encrypted thumbnails. → [details](docs/roadmap/phase-03-image-decode-thumbnails.md) |
| 4 | Graphics layer | ✅ | GPU texture cache and text atlas for the UI. → [details](docs/roadmap/phase-04-graphics-layer.md) |
| 5 | Unlock screen & gallery grid | ✅ | Vault layer connected to the UI — create/open/unlock a vault, browse galleries. → [details](docs/roadmap/phase-05-unlock-screen-gallery-grid.md) |
| 6 | Image viewer | ✅ | Full-screen image viewing with zoom/pan and the auto-scrolling thumbnail strip. → [details](docs/roadmap/phase-06-image-viewer.md) |
| 7 | Hardening & polish | ✅ | Close security gaps, handle edge cases, add deletion + compaction. → [details](docs/roadmap/phase-07-hardening-polish.md) |
| 8 | Cross-platform ports | ✅ | Windows and (later-removed) macOS build configs + CI pipeline. → [details](docs/roadmap/phase-08-cross-platform-ports.md) |
| 9 | Extra image formats | ✅ | WebP and HEIC/AVIF support. → [details](docs/roadmap/phase-09-extra-image-formats.md) |
| 10 | Export (selective, hard-gated) | ✅ | Deliberately extract decrypted images out of the vault, consent-gated. → [details](docs/roadmap/phase-10-export.md) |
| 11 | Slideshow | ✅ | Auto-advancing full-screen viewing of a leaf gallery. → [details](docs/roadmap/phase-11-slideshow.md) |
| 12 | Tags & Search | ✅ | Per-node tags on images and galleries, with cascade + live search. → [details](docs/roadmap/phase-12-tags-search.md) |
| 13 | Favorites | ✅ | Mark images/galleries as favorite, browse via two dedicated screens. → [details](docs/roadmap/phase-13-favorites.md) |
| 14 | Multiple vaults | ✅ | Manage and open several vaults; move images between them. → [details](docs/roadmap/phase-14-multiple-vaults.md) |
| 15 | Video playback (frames + seek) | ✅ | Store videos in the vault, play the video track, seek. → [details](docs/roadmap/phase-15-video-playback.md) |
| 16 | Audio & A/V sync | ✅ | Audio track added to the video pipeline with proper sync. → [details](docs/roadmap/phase-16-audio-av-sync.md) |
| 17 | Import ZIP archives | ✅ | Import a `.zip` of photos/videos into the vault in one action. → [details](docs/roadmap/phase-17-import-zip-archives.md) |
| 18 | Advanced search (dedicated screen) | ✅ | Weighted include/exclude + AND/OR groups + saved searches. → [details](docs/roadmap/phase-18-advanced-search.md) |
| 19 | Gallery cover thumbnails | ✅ | Representative cover art instead of the generic folder icon. → [details](docs/roadmap/phase-19-gallery-cover-thumbnails.md) |
| 20 | Advanced-search list/grid result views | ✅ | Toggle the advanced-search result panel between list and grid. → [details](docs/roadmap/phase-20-advanced-search-result-views.md) |
| 21 | Import a tag list onto a gallery | ✅ | Bulk-add tags to a gallery from a plain-text file. → [details](docs/roadmap/phase-21-import-tag-list.md) |
| 22 | Tag overview screen | ✅ | Dedicated screen listing every distinct tag with counts. → [details](docs/roadmap/phase-22-tag-overview-screen.md) |
| 23 | UI colour schemes | ✅ | Several selectable, runtime-switchable UI themes. → [details](docs/roadmap/phase-23-ui-colour-schemes.md) |
| 24 | Import CBZ archives | ✅ | Import a `.cbz` comic archive as a single page gallery. → [details](docs/roadmap/phase-24-import-cbz-archives.md) |
| 25 | Bugfixes & housekeeping | ✅ | Layout-independent keybindings, unified background-job progress UX. → [details](docs/roadmap/phase-25-bugfixes-housekeeping.md) |
| 26 | Transparent vault compression | ✅ | Adaptive store-if-smaller deflate framing before encryption. → [details](docs/roadmap/phase-26-vault-compression.md) |
| 27 | `meta.json` metadata on archive import | ✅ | Archive `meta.json` seeds the imported gallery's title + tags. → [details](docs/roadmap/phase-27-meta-json-metadata.md) |
| 28 | Broaden `.mov` / video codec support | ✅ | Decode ProRes/DNxHD/MJPEG beyond H.264/H.265. → [details](docs/roadmap/phase-28-mov-codec-support.md) |
| 29 | Tag autosuggest in the tag editor | ✅ | Autocomplete dropdown while typing a tag. → [details](docs/roadmap/phase-29-tag-autosuggest.md) |
| 30 | Import PDF as a gallery of pages | 🔜 | Import a `.pdf` as a gallery of rendered page images, like CBZ. → [details](docs/roadmap/phase-30-import-pdf.md) |
| 31 | Fullscreen viewing + edge-click navigation | ✅ | Borderless fullscreen + click-to-advance in the viewer. → [details](docs/roadmap/phase-31-fullscreen-viewing.md) |
| 32 | Background multi-file import | ✅ | Multi-select file-picker import runs on a background job. → [details](docs/roadmap/phase-32-background-multi-import.md) |
| 33 | Keep a vault unlocked for the session | ✅ | Session-only opt-out of the idle auto-lock timer. → [details](docs/roadmap/phase-33-keep-unlocked-session.md) |
| 34 | Import 7z, RAR, and TAR archives | ✅ | Extends ZIP/CBZ import to `.7z`/`.rar`/`.tar` via libarchive. → [details](docs/roadmap/phase-34-import-7z-rar-tar.md) |
| 35 | Password-protected archive import (ZIP/CBZ) | ✅ | Import a password-protected `.zip`/`.cbz`. → [details](docs/roadmap/phase-35-password-protected-archives.md) |
| 36 | Robust special-character filename & archive-name handling | ✅ | Safe node-name rules + legacy CP437 zip entry-name decoding. → [details](docs/roadmap/phase-36-special-character-filenames.md) |
| 37 | Persisted per-gallery sort order | ✅ | Choose + persist a gallery's sort order, including natural name order. → [details](docs/roadmap/phase-37-persisted-sort-order.md) |
| 38 | WebM video support (VP8/VP9) | ✅ | Import and play `.webm` video (Matroska + VP8/VP9). → [details](docs/roadmap/phase-38-webm-video-support.md) |
| 39 | Discoverable shortcuts & session-scoped gallery memory | ✅ | `F1` help popup + session-scoped gallery/viewer state memory. → [details](docs/roadmap/phase-39-discoverable-shortcuts-session-memory.md) |
| 40 | Video codec/loop/sync polish, gallery position memory & view density | ✅ | Part 1 ✅: AV1 + broader `.mov` codecs, video loop toggle, A/V sync hardening, + bugfix ✅: self-healing metadata repair for videos imported before their codec was decodable. Part 2 ✅: session-scoped gallery position memory (descend/ascend/leave-and-return restores the last-selected tile at every level). Part 3 ✅: 5-way List/Grid S-XL view density. → [details](docs/roadmap/phase-40-video-gallery-browsing-polish.md) |
| 41 | Async video decode | ✅ | Move CPU-heavy video codec decode off the render thread onto a background worker, so slow codecs (AV1/HEVC) don't stall playback/input/A-V sync. → [details](docs/roadmap/phase-41-async-video-decode.md) |
| 42 | ThreadSanitizer CI leg | ✅ | New `--tsan` build option + `tests-tsan` CI job, running the full suite under ThreadSanitizer on every PR to directly validate Phase 41's concurrent code (and any future threading) — reuses the plain vendored codec/SDL3 build rather than a parallel sanitizer-instrumented prefix. The wrapper and CI force VAAPI's null driver so a known race in Mesa's AMD driver cannot terminate and truncate the suite; project hardware-fallback behavior remains covered by deterministic tests. → [details](docs/roadmap/phase-42-tsan-ci.md) |
| 43 | Platform hardware-accelerated video decode | ✅ | Part 1 ✅: shared `media::HwAccelContext` infra + Windows D3D11VA, software `VideoDecodeWorker` as the automatic fallback. Part 2 ✅: VAAPI dlopen shim (`vendor/vaapi-shim`) + Linux enablement (`vendor/libva`, headers-only). → [details](docs/roadmap/phase-43-hardware-video-decode.md) |
| 44 | Gallery organization tools | ✅ | Part 1: scrollable + filterable Move-dialog gallery picker. Part 2: rename images/videos/galleries. Part 3: mass-move extended to galleries. Part 4: combine (recursive merge) two galleries, same- or cross-vault. → [details](docs/roadmap/phase-44-gallery-organization.md) |
| 45 | Organization, security & fullscreen polish | ✅ | Rename extended to favorites/tag-overview/search-result screens, mass tag add/remove, clipboard copy for password/passphrase, fullscreen hides the thumbnail strip, bigger video seek-bar hit target, auto-lock-off badge fades after 10s. → [details](docs/roadmap/phase-45-organization-ux-polish.md) |
| 46 | Mixed galleries (images + videos + sub-galleries together) | ✅ | Relax the leaf-only invariant so a gallery can hold any combination of media and sub-galleries, like a real folder. → [details](docs/roadmap/phase-46-mixed-galleries.md) |
| 47 | Animated GIF support | ✅ | Animated GIFs animate in the viewer (Space pauses) and on the hovered grid/strip tile, and carry an "A" badge. FFmpeg gif decoder + a new `animated` index flag (`INDEX_VERSION` 7). → [details](docs/roadmap/phase-47-animated-gifs.md) |
| 48 | Gallery detail panel | ✅ | A toggleable right-edge panel (`D`, `Ctrl+D` on advanced search) showing the focused node's metadata — type/codec, dimensions, size, date, own + inherited tags — plus a recursive tally for galleries and an aggregate summary for multi-selections. → [details](docs/roadmap/phase-48-detail-panel.md) |
| 49 | Colour-coded tag chips & per-vault settings | ✅ | Tags render as a coloured dot + bare name instead of `category:name`, on every tag surface. New global `F2` settings overlay (sidebar + pane) configures the per-vault category→colour mapping and a vault-wide default sort order; theme moves in from the deleted `C` picker. New vault-global settings block + explicit `Insertion` sort key (`INDEX_VERSION` 8). → [details](docs/roadmap/phase-49-tag-chips-settings.md) |
| 50 | Background import queue | ✅ | All imports (picked files, zip/cbz, 7z/rar/tar) run on a queueable background pipeline while the vault stays fully browsable — main-thread-only index tree, second read-only file handle, thread-safe chunk staging, whole-chunk write-mutex holds, CommitLane with stop-aware coalescing async batched index commits (~32 files / 2 s), parallel thumbnail-decode pool (min(hw,4)), strict in-order resequencing, lookahead cap 8 items/256MiB. New Import Status screen (`Shift+I`: progress, reorder, cancel, lane-failure banner) + live footer summary; idle auto-lock suppressed while queue non-empty, confirm dialog on manual lock/quit with password-at-enqueue for encrypted archives. → [details](docs/roadmap/phase-50-background-import-queue.md) |
| 51 | Tag metadata, folder import & organisation polish | ✅ | Per-tag free-text descriptions shown and edited in the tag overview (new vault-global block, `INDEX_VERSION` 9); tags roll up from a gallery's contents as well as down from its ancestors (read-time, nothing stored); import a folder as a gallery with subfolders mirrored as sub-galleries (`O`, reusing the ZIP tree planner); multi-select for archive and folder picks; direct-child counts on sub-gallery tiles; and an `F1` help popup that stops clipping lines and reflows into two columns. 1229 tests / 0 failed. → [details](docs/roadmap/phase-51-tag-metadata-folder-import.md) |
| 52 | Legacy container & codec support | ✅ | Play the video that dominated 2000–2010: add the AVI, MPEG-PS, MPEG-TS, ASF/WMV, FLV, Ogg and RealMedia demuxers plus MPEG-1/2, MPEG-4 ASP (DivX/Xvid), MS-MPEG4 v1–v3, WMV1/2/3, VC-1, H.263, Sorenson, DV, RealVideo and the lossless/legacy long tail. Adds yadif deinterlacing and honours `sample_aspect_ratio` so anamorphic DVD rips stop rendering squashed. **Note:** raw MPEG-PS (`.mpg`/`.mpeg`) is not decodable in the decode-only build (stores as Unknown-codec); MPEG-1/2 work via MKV/TS/MP4/MOV. Hardware decode (VAAPI/D3D11VA) was a compiled no-op from Phase 43; now fully functional. 1298 tests / 0 failed. → [details](docs/roadmap/phase-52-legacy-video-codecs.md) |
| 53 | Recursive & multipart archive import, gallery select-all | ✅ | Archives nested inside archives extract recursively, each becoming its own sub-gallery (depth cap 16, plus expanded-bytes / live-bytes / count / expansion-ratio guards against zip bombs); multipart sets import as one archive — `.7z.001`/`.zip.001`/`.tar.001` by concatenation, `.partN.rar`/`.r00` via libarchive's file-oriented API, and `.z01`/`.z02` via a new spanned-ZIP merger that rewrites central-directory disk numbers and offsets (libarchive and miniz both refuse multi-disk zips). Volume sets are auto-discovered and confirmed in a dialog that blocks on gaps. `Ctrl+A` toggles select-all over a gallery's direct children. **Known limits:** an encrypted split set fails rather than prompting (encryption is not probeable from one volume), and a multi-volume RAR imports flat. 1451 tests / 0 failed. → [details](docs/roadmap/phase-53-recursive-multipart-archives.md) |
| 54 | Editable input fields (caret, selection, clipboard) | ✅ | Every text field in the app gains a real caret: arrow-key movement, `Ctrl+Left/Right` word jumps, `Home`/`End`, `Shift`-extended selection, `Ctrl+A/C/X/V` and `Shift+Insert`, plus UTF-8-correct backspace (today every field deletes a *byte*). New pure SDL-free `TextInputModel` + `text_field_view` layout, and a `SecureBytes`-backed `SecureTextInput` that retires `SecureTextField` — paste into password fields is allowed, copy/cut out of them is not. All 18 fields (4 secure, 14 ordinary) migrated in one pass; `SecureTextField` is gone. Both backends share one `TextInputBase`, so their caret/selection semantics cannot drift, and one conformance suite runs against both. Two pre-existing defects fall out of the migration: the saved-search name prompt had no Backspace handler at all, and the tag-overview filter could never receive text (its browse-mode gate could not become true) — `/` now opens an explicit filter mode. Known limit: the font atlas bakes ASCII 32–126 only, so non-ASCII stores and pastes correctly but does not render. 1506 tests / 0 failed. → [details](docs/roadmap/phase-54-editable-input-fields.md) |
| 55 | JSON tag dictionary import | ✅ | Import a JSON file of `{category, name, description}` entries to populate the vault's tag *vocabulary* — registering categories with auto-assigned swatches (lowest free palette index, wrapping round-robin past 16) and storing per-tag descriptions in the existing Phase 49/51 vault-global blocks. It tags no images. `Ctrl+I` on the tag overview, exception-free nlohmann parsing per the Phase 27 pattern, one settings commit at the end. Two new pure modules — `tag_json_parse` (bare array or `{"tags":[…]}`, trims every field, rejects a colon in `name`, truncates category/description on a UTF-8 boundary, case-insensitive de-dupe) and `tag_dict_import` (over `VaultSettings`, so the whole import unit-tests without opening a vault). An empty `description` **preserves** any existing one — a deliberate divergence from Phase 51's edit-time "empty removes" rule. No cap is silent: the summary modal reports categories/descriptions added, descriptions updated, entries skipped (malformed / vault full), categories not registered, and fields shortened. **Known limit:** the overview lists tags something in the vault carries, so a description imported for an unused tag is stored but has no row until that tag is used. **No `INDEX_VERSION` bump** — writes only into blocks that already exist. 1556 tests / 0 failed. → [details](docs/roadmap/phase-55-json-tag-dictionary-import.md) |
| 56 | UI layout, import status & navigation polish | ✅ | Six UI defects. One font-derived text pitch (`ui::line_pitch`, `ceil(font_px * 1.25)`) replaces every hardcoded line height, so a scrolled popup can no longer cut its bottom line (the 28 px font was being laid out on 20–25.5 px pitches). Import Status rows become two lines — `source → target` above, progress bar or outcome below — so the bar stops overlapping the text and finished items keep showing where they came from and went; selection becomes id-based so `Ctrl+Up`/`Ctrl+Down` keeps focus on the item it just moved. The viewer re-binds by path instead of refitting on `on_vault_changed`, so a background import's commit no longer resets zoom/pan/scroll or restarts video playback. Right-click becomes a universal "back / up one level" by translating to a synthetic Esc at the single event funnel. Plus a HiDPI fix found while tracing: layout is in render pixels but SDL delivers mouse events in window points, so every hit-test was off by the display scale on Windows. 1636 tests / 0 failed. → [details](docs/roadmap/phase-56-ui-layout-import-status-navigation.md) |
| 57 | Animated WebP support | ✅ | Animated WebP files get the full Phase 47 GIF treatment: the "A" badge in the grid and thumbnail strip, animation in the viewer with `Space` to pause, and 200 ms hover auto-play under the same 1920×1080 / 300-frame budget. Also a bug fix — animated WebPs cannot be imported **at all** today (`WebPGetInfo` returns the `VP8X` canvas size so the size check passes, then `WebPDecodeRGBInto` fails on the `ANIM`/`ANMF` payload), which also means no existing vault can contain one. Decoding uses libwebp's own `WebPAnimDecoder` (`libwebpdemux.a` is already built and vendored), so unlike GIF it needs **no FFmpeg** and works in every build. Phase 47's format-neutral machinery is generalized rather than duplicated: `gif_playback`/`gif_model`/`gif_repair`/`gif_info` become `anim_*` over a new `media::AnimDecoder` interface with `GifDecoder` and a new `WebpAnimDecoder` behind it. Transparency is flattened over black (matching how the app already treats transparent PNGs/GIFs) and the file's loop count is ignored, so playback matches GIF exactly. **No `INDEX_VERSION` bump** — `ImageMeta::animated` was already format-neutral. One new predicate, `vault::format_can_animate`, is the single source of truth for import, repair, the sniff gate, the badge and the hover gate. Fixed a pre-existing break found while restructuring: `gif_playback.cpp` opened `namespace ui` *inside* its `#ifdef OSV_VENDORED_AV`, so that translation unit did not compile at all without vendored FFmpeg — Phase 47's non-FFmpeg fallback was never buildable, and no CI job would have caught it. 1677 tests / 0 failed. → [details](docs/roadmap/phase-57-animated-webp.md) |
| 58 | Big-vault responsiveness & import-time UI stability | ✅ | Render thread no longer performs synchronous vault I/O for thumbnails: `DecodeWorker::submit_fetch` adds a worker-thread fetch stage (vault-agnostic `Fetcher` callable), `Vault` gained a dedicated async-safe background `thumb_fp_` handle under mutex, and read failures are memoized to prevent retry loops. UI refresh no longer re-resolves cover spans per frame — `CoverCache` computes them once per listing, invalidated on gallery refetch. Search overlay separated into gather-once (vault walk) and filter-per-keystroke (predicate over cached hits), debounced to 150 ms input silence on advanced search only. Gallery refresh during batch import preserves scroll via `remap_listing` — selection re-keyed by name, not tile identity. Collection-mode viewers (favorites, tag overview, search results) re-resolve their album `IndexNode*` on vault change, fixing dangling pointers. App::update extracted from run() to host perf tracing. Instrumentation added: `platform::PerfScope` RAII tracing (`OSV_PERF_LOG=1` env flag) with fixed labels for 20 ms frames, app.update, grid refresh/detail, search gather, advanced-search rerun, 5 ms thumbnail fetch, and vault-rebind fallback. **Root causes fixed:** (a) uniform 0.5–1 s UI latency on 200 GB / 50k+ item vaults — render thread doing per-frame seek+read+decrypt per missed thumbnail, per-frame cover re-resolution and failed-read retry, search per-keystroke vault walk; (b) video playback/scroll reset during imports — grid `on_vault_changed` cleared selection state, collection viewers held dangling node pointers. 1702 tests / 0 failed. → [details](docs/roadmap/phase-58-big-vault-responsiveness.md) |
| 59 | Non-blocking video seek | ✅ | Seeking (J/L, frame-step back, seek-bar click/drag, loop-at-EOF, session resume) no longer freezes the render thread: `do_seek()` used to run the seek's decode-forward — from the anchor keyframe to the target — to completion inside the blocking `decode_into_pending()`, which on long-GOP / slow-software-codec clips (AV1, HEVC) meant seconds of unpumped events and a compositor "not responding" flag; scrubbing fired that per mouse-motion event. Now `do_seek()` is a pure state transition (demux reseek, worker `begin_seek`, generation bump, audio re-base, transport jump) that returns immediately; the previously shown frame stays up until the target frame lands. The existing `skip_pending_` flag drives the rest: `animating()` holds true while a seek is resolving so `App::run` keeps ticking even when paused, `try_advance_pending()` tops the worker up to a deeper `SEEK_FEED_DEPTH` (32) and feeds uncapped on timeout while it is set, and `consume_result()` realigns the transport to the decoded frame's actual pts on resolve. Only the constructor's frame 0 still uses the blocking path (pts 0 is a keyframe — no gap). No new threads, no new shared state, no format change. 1714 tests / 0 failed. → [details](docs/roadmap/phase-59-async-video-seek.md) |
| 60 | In-place vault compaction (dead-space packing) | ✅ | `Vault::compact()` now packs live chunks into dead space in place — O(1) extra disk instead of the old ~2x copy-rewrite, so a 200 GB vault no longer needs another 200 GB free to compact. Crash-safe by construction: moves land in dead space per the last-committed index; batch commits use the existing 3-phase slot-swap with no journal or recovery path. Cancellable and resumable: Esc keeps completed work; re-running continues. Final index blob is placed just after the packed data, dead tail is truncated, residual holes punched (Linux). Copy-rewrite machinery deleted; Windows auto-reclaim no longer transiently doubles disk use. 1724 tests / 0 failed. → [details](docs/roadmap/phase-60-in-place-compact.md) |
| 61 | Duplicate finder | ✅ | `Ctrl+D` on the gallery grid opens a whole-vault duplicate scan — exclusive op behind the same import-queue gate as compact, with a new "Vault tools" F1 group. The screen opens in a chooser state: **exact** (group by type+size from a main-thread snapshot, then BLAKE2b over the full plaintext decrypted into mlock'd `SecureBytes` and wiped immediately — only size-colliding files are ever decrypted) or **exact + visually similar** (64-bit dHash over the stored thumbnails, union-find clustered at Hamming ≤ 5, images only). The background `DupScanJob` only ever calls the thread-safe `vault::read_thumb_span` over the snapshot — the main-thread-only index tree is never touched off-thread; bad files are skipped and counted, manual lock cancels the worker before key wipe, and hashes are session-lifetime heap data (never persisted, never logged — `INDEX_VERSION` stays 9). Review lists groups largest-reclaimable-first as side-by-side tiles (name, parent path, size, resolution) with KEEP/REMOVE badges: `Space` toggles, `A` keeps only the focused member, `Enter` full-screen inspects the decoded original, and a pure-model invariant blocks apply while any group is all-REMOVE (that would be deletion, not de-duplication). `Ctrl+Enter` → default-cancel confirm → new vault free friend `vault::remove_media_batch` — N erases, ONE `commit_index()` + one `auto_reclaim_space()`, i.e. one crash-safe slot swap instead of one fsync per file. 1757 tests / 0 failed. → [details](docs/roadmap/phase-61-duplicate-finder.md) |
| 62 | Perceptual video duplicates | ✅ | Phase 61's "Exact + visually similar" scan now catches re-encoded / resized / remuxed copies of the same video (before, only byte-identical videos grouped). Three-stage funnel, cheapest evidence first: duration gate from `vmeta.duration_us` (±2 %, 500 ms floor — zero I/O), poster dHash prefilter (stored first-frame JPEG, Hamming ≤ 10), then a frame confirm that software-decodes 5 frames at 10–90 % of the timeline and requires ≥ 4 of 5 per-position dHashes within Hamming ≤ 7. Frame decode runs through a new `ui::compute_video_frame_sig` over a callback AVIO backed by a chunk-caching `vault::read_thumb_span` byte stream — the worker never touches the main-thread `read_fp_`/`VideoSource`. Union-find clusters matching pairs into "Similar video" groups; review/marking/apply unchanged; non-FFmpeg builds accept the duration+poster verdict (stub). No new UI mode, no `.osv` change. Fixtures: the same 2 s `testsrc2` clip pre-encoded as H.264/MP4 **and** VP9/WebM (decode-only FFmpeg cannot re-encode at test time; lavfi `gradients` was rejected — it randomizes colors per invocation). Found & fixed in the decode loop: after the demux-EOF flush, `av_packet_unref` resets `stream_index` to 0, and sending that stale packet errored out late timeline positions on single-keyframe files. 1774 tests / 0 failed. → [details](docs/roadmap/phase-62-perceptual-video-duplicates.md) |
| 63 | Vault growth on unlock (Windows) & duplicate-review UX | ✅ | Two owner-reported defects. **(a) Old vault grows + lags on every unlock:** `repair_video_metadata()` appended the re-probed poster chunk on `fp_` *without* `write_mutex_`, racing the CommitLane worker (started at unlock) committing the previous repair's index blob on the same `FILE*` — the interleaved seek+write could misplace `write_header`'s payload at EOF (the PR #109 failure class), losing the active-slot flip, so the next unlock loaded the stale slot, saw the videos as Unknown again, and re-ran the repairs — appending new posters + index blobs every session. Windows widens the race (slow `FlushFileBuffers`) and never hole-punches the dead bytes. Fixed by holding `write_mutex_` across the append; the silent slot-fallback in `unlock()` now logs; and a transient `VideoMeta::probe_failed_session` memo stops gallery refresh re-reading whole undecodable videos on every visit. Regression-tested by repairing 8 forced-Unknown videos under a live CommitLane and cold-reopening. **(b) Duplicate review:** groups now default to KEEP on the first member with the rest pre-marked REMOVE (an untouched review applies straight to one-copy-per-group); `Space` can no longer unmark a group's last keeper (all-REMOVE unreachable, refusal shown in the footer); leave-confirm + idle-lock-block gate on user-touched marks; and the full-screen inspect texture is now owned by the screen instead of passing through the shared thumbnail cache, whose eviction was blanking the preview (and the tiles) to black — the `FullTexCache` rationale applied to inspect. 1787 tests / 0 failed. → [details](docs/roadmap/phase-63-unlock-growth-dup-review-ux.md) |
| 64 | Wave-based duplicate review & review-screen memory fix | ✅ | Two independent fixes. **(a) Review-screen memory churn:** `render_review` drew every group row each frame with no culling, and submitted decode fetches for every member; once the thumbnail set exceeded the 256 MiB `TextureCache` LRU the screen churned indefinitely — decrypt → decode → evict → refetch thousands of times per pass. Fixed: cull off-screen rows, only visible tiles fetch, and `DecodeWorker::retain(visible ∪ inspect)` each frame to prune stale requests. RAM now bounded by visible tiles independent of group count. **(b) Wave UX:** review presented in 20-group waves resolved incrementally — `Ctrl+Enter` applies the current wave (one `remove_media_batch` + accumulate totals), `N` skips a wave with confirm. Post-apply span re-resolution re-reads all surviving members by path (Windows `compact()` relocates chunks) — `failed_` memo cleared, groups shrinking below 2 members dropped. Header shows wave N/M, this-wave groups, bytes reclaimable, total remaining. No `.osv` change, no `INDEX_VERSION` bump. 1799 tests / 0 failed. → [details](docs/roadmap/phase-64-dup-wave-review.md) |
| 65 | Blocking vault migration | ✅ | One-time blocking, parallel, batch-committed upgrade pass offered at unlock, replacing the lazy in-browse repairs so a `.osv` stops mutating while browsing. Watermark in `VaultSettings` (`INDEX_VERSION = 10`). **Also (pre-existing Phase 50 defect):** `ImportQueue::maybe_end_batch()` was running `enqueue_snapshot()` + `flush()` on **every idle frame**, appending a full index blob each time and growing the vault at ~795 bytes/second with no user input — unbounded since Phase 50, affecting every user. Measured test vault: 45,775 → 248,755 bytes in ~7 minutes idle. Fixed by `batch_ended_` latch, firing once per busy→idle transition (re-armed when work arrives, reset in `begin_session()`). Frozen vault demonstrated: `45979 1785937476` at t=0/5/10/20/35 s idle. → [details](docs/roadmap/phase-65-blocking-migration.md) |
| 66 | Keep the destination vault open after cross-vault copy/move | ✅ | Cross-vault Copy/Move/Combine can keep the unlocked destination vault warm in a single app-owned slot (`app::SecondVaultSession`) instead of wiping its key on dialog close. Three modes chosen at the destination-unlock stage — **Lock immediately** (default, today's behavior), **Keep open 5 minutes** (sliding window, reset by every completed transfer into it), **Keep open for the session** (explicit lock only) — seeded from a persisted per-machine `F2` default. A warm vault skips the password stage in Transfer/Combine and opens password-free from the vault manager / quick-switch; an App-level badge with live countdown is visible whenever the second key is in memory. Session-only and in-memory: the first unlock each session always requires the password. No `.osv` change. 1836 tests / 0 failed. → [details](docs/roadmap/phase-66-keep-destination-vault-open.md) |
| 67 | Cross-vault transfer robustness | ✅ | Per-file transfer tolerance: `transfer_images` and `transfer_gallery` record failures (path, code, stage) in a `TransferTally` (up to 100 entries, counter unbounded) rather than aborting on the first error — partial transfers commit and the gallery grid opens a new `ui::FailureListDialog` listing failed items with ASCII reasons. Metadata preservation: transfers carry `created_ts`, `format`, dimensions, and encrypted thumbnails from source to destination verbatim — no re-probe (video metadata) or re-decode (images/video poster), except when restaging plaintext (image adds precompute dimensions from IHDR if available). **Staging API expands:** `vault::StagedVideoInfo` holds precomputed metadata (duration, dimensions, format) for video pre-staging; `stage_video()` takes a 5th `precomputed` param to bypass the probe; free friends `add_image_prestaged` / `add_video_prestaged` accept pre-staged chunks and metadata, closing a staged-media ingress for transfers. **Per-file Move semantics:** `transfer_gallery` copies each file individually then removes it from source (per-file Move), followed by bottom-up pruning of empty galleries from the source tree (residue pruning) — **after** all files succeed; on cancel, intermediate partial Moves stay (transferred files removed from source, untransferred files remain), no duplicates, no data loss. **Cancel semantics change:** modal confirm on gallery transfer cancel now shows the clean partial-move outcome clearly. No `.osv` change, no `INDEX_VERSION` bump. 1842 tests / 0 failed. → [details](docs/roadmap/phase-67-transfer-robustness.md) |
| 68 | Import-error resilience, multiselect everywhere & browsing polish | ✅ | Corrupt-archive import no longer kills the app: task-boundary catch on the import worker + decode pool → **Failed** task with reason + one `[Import]` line in `error.log`, a corrupt ROOT archive fails loudly instead of folding into `skipped`, lying zip size headers are refused before allocation (`zip_entry_size_plausible`), and a Phase 50 write-back defect (worker results discarded — failed archives rendered "Done") is fixed. Multi-select drives favorite toggle (`B`, batch via new `vault::set_favorites_batch`, one commit), export, and move/copy on the grid — mixed images+videos+galleries in one run (videos were silently dropped by an `is_image()` filter) — and full Space/`Ctrl+A` + `B`/`X`/`M` lands on the favorites/tag/search screens via a shared `ui::CollectionBatchOps` (per-parent grouped transfers). Mouse wheel scrolls the viewer thumbnail strip (manual override, re-centers on navigation; works during video playback) and the saved-search sidebar. `n / N` position counters: grid chrome line, favorites/tag/search headers, and a viewer strip-edge badge that stays visible in fullscreen. 1895 tests / 0 failed. → [details](docs/roadmap/phase-68-import-resilience-multiselect-polish.md) |
| 69 | Batched cross-vault transfer commits | ✅ | Vault-to-vault transfer was an order of magnitude slower than import: every file paid a synchronous durable index commit at the destination (chunk fsync + 3-sync slot swap + full index serialization), and Move mode paid a second commit **plus** an `auto_reclaim_space()` pass on the source per file — and since `reclaim()` never lowers the logical `wasted_bytes()` measure, once a big move crossed the ¼-waste gate every subsequent per-file remove re-ran the full live-span scan + hole-punch. Transfers now batch like the import queue: the prestaged ingress splits into `attach_image_prestaged` / `attach_video_prestaged` (stage + attach, **no** commit) and `commit_staged` (one chunk fsync + one crash-safe commit), the bulk drivers flush once per 32-file batch (`TRANSFER_COMMIT_BATCH`), gallery recreation rides the batch via `ensure_gallery_path` (no per-gallery `create_gallery` commit), and Move-mode source removals are deferred into ONE `remove_media_batch` after the destination is durable (single source commit + single auto-reclaim). Vault combine's leaf loop routes through the batched `transfer_images` (new `TransferProgress{.set_total=false}` option protects its subtree-wide progress total). Semantics preserved: destination durability still strictly precedes any source mutation (crash ⇒ recoverable duplicates, never loss); a batch-commit failure fails exactly that batch's files and hard-stops the transfer; cancel flushes files copied so far and still removes them from the source — no duplicates. Observable via the new `fileutil::sync_call_count` counter: a 10-file Move is now ≤ 12 syncs (was ~70). 1902 tests / 0 failed. → [details](docs/roadmap/phase-69-transfer-batching.md) |
| 70 | Unicode-safe paths | ✅ | Fixes the Windows import crash (`system_error`: no mapping in the multi-byte code page) caused by `path::string()` converting native UTF-16 paths through the ANSI code page. New `platform/path_utf8.h` (`utf8_to_path` / `path_to_utf8` / `path_to_utf8_generic` / `fopen_path` via `_wfopen`); app-wide sweep of every `path::string()`, narrow `path{}` construction, and `fopen(path.string())`. Convention: `std::string` paths are UTF-8 by definition; narrow conversions banned. Drift additions: `freopen_path` helper, `folder_scan.cpp` + `harden.cpp` swept. 1911 tests / 0 failed. → [details](docs/roadmap/phase-70-unicode-safe-paths.md) |
| 71 | Transfer conflict resolution | ✅ | Gallery transfers hitting a same-named destination gallery now ask once per run: **Combine** (merge via `combine_galleries` — files skip, same-named sub-galleries recurse; Copy-capable) or **Rename** with a `_2` suffix. File-name collisions become counted **skips** everywhere (status shows "(N skipped)"; no failure-list entries). New `vault::CollisionPolicy`, `GalleryTransferOpts`, `colliding_galleries` pre-scan, suffix probing with UTF-8-boundary trimming at the 255-byte name cap. TransferDialog `Conflict` stage; tests in `test_transfer_conflicts.cpp` + Copy-combine tests + skip-reporting tests. 1923 tests / 0 failed. → [details](docs/roadmap/phase-71-transfer-conflict-resolution.md) |
| 72 | Unicode dialog paths & CJK archive entry names | ✅ | Phase 70's sweep missed the dialog callbacks themselves: `FileDialog::on_files` / `FolderDialog::on_folder` still rendered every picked path with `path::string()` — on Windows an ANSI-code-page conversion that throws `std::system_error` for CJK names, unhandled inside the SDL callback ⇒ the "import of archives and files crashes on Japanese/Chinese names" report. New `platform::normalize_external_path_utf8` (normalize_user_path → path_to_utf8) is now the single sanctioned dialog→string conversion; consumer sweep of the remaining narrow `fs::path` constructions (gallery_grid picks, export_ui, unlock_screen vault/keyfile, vault_unlock_picker keyfile, collection_ops export, image_viewer export status, `config_dir`'s SDL pref path). Second bug, same report: libarchive converts 7z/RAR entry names (UTF-16 in-header) through the current locale at parse time — under the never-set `"C"` locale every accessor returns NULL and each CJK entry collapsed to "unnamed". Fixed by `platform::init_locale()` (LC_CTYPE-only → UTF-8, app + test main; deliberate no-op on Windows, where a non-"C" CRT locale would make libarchive OEM-decode raw tar names) plus `ArchiveReader`'s `entry_name_utf8` (narrow-if-valid-UTF-8, else wide converted locale-independently via `fs::path` — the Windows path, where libarchive always keeps the wide name), `ui::is_valid_utf8` exposed from zip_encoding, and Windows multi-volume opens via `archive_read_open_filenames_w`. Committed real-7-Zip CJK fixture (`cjk_names.7z.uu`) — libarchive's own writer cannot produce CJK names under `"C"`. 1929 tests / 0 failed. → [details](docs/roadmap/phase-72-unicode-dialog-paths.md) |
| 73 | Tag category templates & non-blocking tagging | ✅ | A category can define template fields; a brand-new tag of that category prompts a skippable fields sheet with per-(category,field) dropdown suggestions; field values match like tags in plain + advanced search (virtual `field:value` tags, match-only). Tagging is non-blocking: `add_tag_batch`/`remove_tag_batch` (one commit per batch) + an app-owned persistent CommitLane (fsync off the UI thread; import queue borrows it). Index v11. → [details](docs/roadmap/phase-73-tag-templates.md) |
| 74 | Multi-select batch delete | ✅ | Del joins the Phase 68 convention: it acts on the Space/`Ctrl+A` selection (focused tile when empty) on the gallery grid, the favorites/tag screens, and advanced-search Results focus — a mixed selection (images + videos + gallery subtrees) deletes in ONE index commit via new `vault::remove_nodes_batch` (free friend beside the untouched media-only `remove_media_batch`), run on the FileOpJob worker (`start_delete_batch`) behind an aggregate default-cancel DANGER modal ("Delete N selected items?" + "2 galleries · 7 images · 312 MB"; summary snapshotted at Del time, paths rebuilt at confirm time from the live selection). Shared pure helpers `ui/batch_delete.*` (descendant pruning, recursive tally, counts line, modal drawer) keep grid and `CollectionBatchOps::request_delete` numbers identical; Phase 50 import-queue exclusivity throughout. Also fixes a latent Phase 68 bug: advanced search never rendered CollectionBatchOps modals while not busy. 1965 tests / 0 failed. → [details](docs/roadmap/phase-74-multiselect-batch-delete.md) |
| 75 | Pull transfers, tag persistence, 512px thumbnails | ✅ | **(a) Pull transfers:** TransferDialog gains a **Direction** stage (`M`: *To another vault* = the unchanged push flow; *From another vault* = pull). Pull: Move/Copy → pick + unlock the SOURCE vault (`VaultUnlockPicker::open` gains `include_self=false`; Phase 66 warm slot works symmetrically as source) → multi-select source galleries (`GalleryPickerModel` opt-in multi-select — Space toggles, `/` filter; descendant selections absorbed via `ui::drop_descendant_paths`; list from new `vault::all_galleries`) → Phase 71 Conflict pre-scan (Combine / Rename `_2` / Cancel) → `FileOpJob::start_transfer_galleries` with src/dst roles swapped. Destination is the currently browsed gallery; completion status reads "from <vault>". Media directly in the source root is not pullable (documented limitation). **(b) Tag & favorite persistence:** every transfer path (single-file, bulk, subtree, combine media; within- and cross-vault) carries new `vault::NodeExtras{tags, favorite}` through the prestaged attach API inside the Phase 69 batched commits (sync budgets unchanged). Media carry their source-side **effective tags** (own ∪ root ∪ ancestor gallery tags, ci-deduped via the newly shared `vault::tag_ci_equal` + `vault::effective_tags`); a transferred subtree ROOT materializes its ancestors-above tags as plain own tags (internal inheritance travels intact); recreated galleries keep own tags + favorite with union semantics (rerun-idempotent). **(c) 512px thumbnails + bigger tiles:** `image::THUMB_MAX_SIDE = 512` for stored thumbs and video posters; tile sizes S 128→192, M 188→256, L 248→352, XL 320→448; one-time regeneration for existing vaults via the Phase 65 MigrationJob (new thumb arm: images re-thumbnailed from originals, known-codec posters re-encoded, resolved-Unknown videos get replacement posters) behind the Index v12 `migrated_thumb_side` watermark, stamped at `Vault::create` so fresh vaults owe nothing. 1999 tests / 0 failed. → [details](docs/roadmap/phase-75-pull-transfers-tag-persistence-bigger-thumbs.md) |
| 76 | Copy-capable gallery combine (Shift+M) | ✅ | Closes the last gap in "move/copy with gallery merging works everywhere": the dedicated combine dialog (`Shift+M`) was Move-only — no Move/Copy stage, and `FileOpJob::start_combine` never passed a mode, so the Phase 71 Copy-combine capability was unreachable from it (the `M` transfer dialog's Conflict stage already offered mode-aware Combine on every surface, pull included). `Shift+M` now asks **Move** (merge, delete the emptied source — historical behavior, still default) or **Copy** (union into the destination, source fully untouched) before picking the destination; the mode threads through `start_combine` into `combine_galleries`, outcome wording follows the verb ("N copied" vs "N moved"), and a Copy of an empty source no longer mis-reports the source as gone. Mode/Direction rows are now one shared `ui::draw_option_rows` widget so Transfer and Combine dialogs cannot drift apart. Stale `combine.h` docs claiming mixed galleries (media + sub-galleries as siblings) are "structurally incompatible" corrected — they combine fine, now regression-tested cross-vault in Copy mode. 2002 tests / 0 failed. → [details](docs/roadmap/phase-76-copy-combine.md) |
| 77 | Vault-upgrade fsync storm & stale progress label | ✅ | Owner report: the vault-upgrade modal hung near the end with the counter maxed (`N / N`) and the title stuck on "Preparing…". Two bugs, both surfaced by Phase 75's thumbnail-regen migration arm: **(a)** `apply_image_thumb`/`apply_video_poster`/`apply_video_probe`'s poster arm each `fsync`'d once per item on the migration coordinator thread — and Phase 75's 512px bump makes every pre-existing image/video's thumbnail stale, so a real upgrade applies one of these per item in the whole library, unlike every other bulk-write path in this codebase (Phase 50 import, Phase 69 transfer batching, `compact()`), which batches. Fixed with a `sync` parameter (default `true`) the migration coordinator now passes `false` at every call site — correct because `commit_migration()`'s own final `commit_index()` sync already flushes every buffered append on the same `FILE*` before the index that references them goes live, cancel included. **(b)** `draw_migration_progress`'s phase-label switch had no `Done` case, falling to the `Idle`/`Scanning` default of "Preparing…" — exactly what made a finished-or-nearly-finished run read as still starting. The label logic moved into a pure, unit-tested `ui::migration_progress_text()`; `Done` now reads "Finishing…". No `.osv` change. 2009 tests / 0 failed; ASAN clean. → [details](docs/roadmap/phase-77-migration-fsync-storm.md) |
| 78 | Side-by-side dual gallery (F3 split view) | ✅ | `F3` toggles a 50/50 split-pane view (two full gallery grids side by side over the same vault; Tab or mouse-click to switch panes); `M` moves or copies the grid selection to the other pane's gallery (Move/Copy modal + Phase 71 conflict resolution + one `FileOpJob` run). Session-only restore of split configuration via `DualSessionState` (split_active + has_config separation ensures exact rehydration after viewer round-trip). Exclusive ops (Shift+M, Ctrl+D, Shift+C, quick-switch) disabled in split mode. MIN_SPLIT_WIDTH 900px enforced via dual_layout viewport-rect embedding; key gating in embedded mode. No `.osv` format change, `INDEX_VERSION` stays 12. 2029 tests / 0 failed / ASAN clean. → [details](docs/roadmap/phase-78-dual-pane-gallery.md) |
| 79 | Idle auto-lock fired during vault upgrade | ✅ | Owner report: a ~400 GB upgrade sat on "Finishing… N/N" for over an hour, zero CPU, idle SSD. Root cause: the 5-minute idle auto-lock was never suppressed while a `MigrationJob` ran — the job is App-owned, so neither `Screen::blocks_idle_lock` nor `ImportQueue::busy()` (the job takes *exclusivity*, not tasks) covered it, and the upgrade's long tail (batched `commit_index()` fsync + whole-file `compact()`) easily exceeds 5 idle minutes on a big vault. The lock tore the vault down under the running coordinator (use-after-free window; the reported run failed cleanly with `Locked`), and the `take_outcome()` poll in `App::update` — gated on `vault_state_.active`, now null — never collected the outcome, wedging the progress modal permanently (the Phase 77 report's "stuck at Preparing… N/N" carried the same signature). Fixed: `should_auto_lock` gains `migration_active` (suppress + reset, unit-tested); the outcome poll is un-gated from the vault (defense in depth); an auto-lock firing on the not-yet-started upgrade *offer* now closes the offer and releases import exclusivity; new `MigrationJob::abort_and_join()` (cancel + join, outcome discarded) runs in `App::shutdown()` and the destructor, so closing the window mid-upgrade can no longer tear the vault down under a live coordinator. No `.osv` change. 2013 tests / 0 failed; ASAN clean. → [details](docs/roadmap/phase-79-migration-autolock.md) |
| 80 | sws_scale heap overrun (tight destination buffers) | ✅ | Owner report: the re-run 400 GB upgrade crashed silently on Windows mid-repair — Event Viewer `0xc0000374` (heap corruption) in ntdll. Root-caused from a full-memory minidump without PDBs (scan-walk → in-dump PE `.pdata` function attribution → RIP-relative string identification): a migration worker inside `VideoDecoder::decode_poster_rgb` → `sws_scale`. The poster path allocated its RGB24 destination with `align=1` — an exactly-sized buffer — but libswscale's vectorized writers store whole SIMD vectors per row: a canary sweep against the vendored build measured overshoot in **1179** format×flags×width combos (worst **42 bytes** past the end; up to 16 past even a 64-aligned `linesize*h`). On Windows, FFmpeg's `av_malloc` hides its base pointer in the inter-block gap, so the overrun poisons the *next* block's pointer and the later `av_free` trips `RtlFreeHeap` — on Linux (posix_memalign + glibc slack) the same overrun is silent, and ASAN can't see stores made by uninstrumented vendored asm, which is why 2000 tests never caught it. Fixed by the FFmpeg-internal convention: padded destinations (`FFALIGN(w*3,64)` linesize + 128-byte tail) with a stride-aware row copy into the tight result — in `decode_poster_rgb` (also now using the decoded frame's real geometry, not open()-time codecpar) and in the second audited instance, the animated-GIF playback decoder (RGBA, 56-byte measured overshoot). New odd-stride fixture (106×64) + probe test; `osv_tests <filter>` substring filter added for single-test valgrind/gdb runs. `FrameConverter::to_i420` audited clean (writes into padded `av_frame_get_buffer` memory). No FFmpeg patch (standard libswscale behavior; a vendored patch would be lost on bumps). 2014 tests / 0 failed; ASAN clean. → [details](docs/roadmap/phase-80-poster-heap-overrun.md) |
| 81 | Animated GIF/WebP render: wrong colours & squashed aspect | ✅ | Owner report: GIFs' "colors and resolutions look wrong compared to the generated thumbnail" — a real side-by-side, since hovering an animated tile swaps the static thumbnail for live playback in the very same rectangle (grid tiles, list rows, the viewer's thumbnail strip). Two bugs, both in the *draw* half of `ui::AnimPlayback`; the decoders were correct throughout. **(a) Colour:** the streaming texture was created as `SDL_PIXELFORMAT_RGBA8888`, a **packed** format that on little-endian means `A,B,G,R` in memory — but both backends emit **byte-order** R,G,B,A (`sws_scale` to `AV_PIX_FMT_RGBA`; libwebp `MODE_RGBA`). SDL's byte-order alias is `SDL_PIXELFORMAT_RGBA32` (= `ABGR8888` on x86), which `gfx/text.cpp` already used correctly for its identically-laid-out glyph atlas. Effect, measured against vendored SDL3: displayed R = source **alpha** (pinned to 255), G ↔ B transposed, and alpha = source red — which under the texture's default `BLENDMODE_BLEND` also faded low-red regions into the background. A deep blue `(10,60,200)` rendered as orange `(255,200,60)`. Fixed to `RGBA32` + explicit `BLENDMODE_NONE` (frames are opaque by construction, so the draw no longer depends on decoder alpha). **(b) Aspect:** `render()` passed its rect straight to `SDL_RenderTexture`, which **stretches to fill**, while every static thumbnail path aspect-fits and letterboxes (`ui::draw_tile_thumb` → `fit_rect`, `gfx::draw_thumbnail_strip`). The three hover sites hand it the whole **square** cell, so non-square animations were squashed on hover. Fixed inside `render()` via the existing `ui::fit_rect` + a black backing painted only when a band actually exists — reproducing the static tiles' letterbox on hover while staying a strict no-op in the two viewer paths (whose rect already carries the image's aspect), so no seam can appear around a zoomed image. No call site changed. **Why 2000+ tests missed it:** nothing had ever called `AnimPlayback::render` — coverage stopped at decode/advance/loop, so the texture format and destination rect were untested from Phase 47 (GIF) and Phase 57 (WebP) onward. Now covered by software-renderer readback tests (exact, lossless fixtures) plus a byte-order pin on the GIF decoder's output. No `.osv` change; `INDEX_VERSION` stays 12; no vault repair needed — nothing wrong was ever stored, only drawn. 2054 tests / 0 failed; ASAN clean. → [details](docs/roadmap/phase-81-anim-render-colour-aspect.md) |
| 82 | `Shift+C` refused to compact a vault with real waste | ✅ | Owner report: "I deleted 1000+ files and it still says no significant space to reclaim." The compact machinery was never at fault (`Shift+C` → `confirm_compact` → `FileOpJob::start_compact` → `Vault::compact` was correct and covered); the **gate in front of it** was. `handle_shift_c_key` reused `ui::should_display_waste` — the Phase 26 predicate written for the *passive footer hint*, `waste >= max(50 MiB, size/10)` — as the permission gate for an explicit keypress, inverting its purpose: the larger the vault, the more waste it takes to be allowed to reclaim any. Together with `auto_reclaim_space`'s own `waste * 4 >= size` gate that leaves a dead band with no way out — on the reported 699 MiB vault, auto-reclaim needs 175 MiB and the keypress needs 69.9 MiB, so ~50 MB of orphaned chunks from a thousand small deletes was neither reclaimed nor offered, with no figure shown either way. Confirmed on the reported vault: logical 732,938,372 B vs 731,852,800 B allocated (`filefrag`: 105 extents, ~1 MiB of holes) — auto-reclaim had long since stopped firing. Fixed with a new pure `ui::should_offer_compact(waste)` (any non-zero waste), safe because the modal it opens already states the exact amount and defaults to cancel — the user makes the I/O trade-off, not the app. Refusal now only fires at true zero ("Nothing to reclaim — the vault has no wasted space."). `should_display_waste` is untouched and still governs the footer hint. Deliberately unchanged: `auto_reclaim_space`'s ratio gate (loosening it puts a full scan on every delete), and `vault_reclaim()`'s missing tail truncation (crash-safety-critical path, own phase). No `.osv` change; `INDEX_VERSION` stays 12. 2057 tests / 0 failed; ASAN clean. → [details](docs/roadmap/phase-82-compact-gate.md) |
| 83 | Every non-ASCII character in the UI rendered as nothing | ✅ | Owner report: "in the F2 menu the are navigation hints that are basically empty []". `gfx::FontAtlas` baked exactly one contiguous range — printable ASCII 32..126 — and BOTH `measure()` and `build_text_geometry()` **silently skipped every byte outside it**. Every byte of a UTF-8 multi-byte sequence is >= 0xC2, so no non-ASCII scalar could ever draw; because `measure()` skipped them too, layout stayed self-consistent (nothing overflowed or misaligned, the characters were simply absent) — which is how this survived 2054 tests and 82 phases. A literal scan found **14 codepoints in ~120 places** all invisible: 45 `—`, 28 `·`, 26 `…`, 17 arrows, plus `✓ ✗ − ★ • ▲ ▼`. It also mangled **vault node names**, which are arbitrary user text drawn straight through `draw_text` — `café.jpg` rendered as `caf.jpg`, and two differently-named non-Latin files rendered identically blank. **(a) Atlas:** `bake()` moves from `stbtt_BakeFontBitmap` to `stbtt_PackBegin`/`stbtt_PackFontRanges` with an explicit codepoint array; coverage is declared as `EXTRA_RANGES` (Latin-1, Latin Ext-A/B, Greek, Cyrillic, General Punctuation, Currency, U+2212) and **filtered against the font's own cmap** via `stbtt_FindGlyphIndex`, so naming a block the bundled font lacks costs nothing and can never pack a blank `.notdef` with a bogus advance. ASCII stays a dense subtraction-indexed array; the rest is a codepoint-sorted vector binary-searched per glyph. Oversampling stays 1x1 so `packedchar` carries exactly the `bakedchar` semantics — existing ASCII metrics byte-for-byte unchanged — and `text_top_for_center` still measures ASCII ink only, so extended coverage cannot shift the vertical centring of every existing screen. `measure()`/`build_text_geometry()` now decode UTF-8 with a decoder hardened for untrusted node names: every failure path consumes exactly one byte (no input can hang it) and overlongs/surrogates/out-of-range are rejected rather than decoded, so a `/` smuggled in as `C0 AF` cannot alias the `/` glyph. Unmapped scalars render one `?` each rather than vanishing. Atlas grows 512² → 1024² at the app's 28 px (one atlas process-wide). **(b) Font gaps:** the bundled `NotoSans-Regular.ttf` is a 2965-codepoint subset with **no** arrows, checks, stars or geometric shapes, so nine call sites are respelled in ASCII (`[Up/Dn]`/`[Lt/Rt]`, `Name asc`/`desc`, `* favorite`, `^`/`v`, `->`, `Done —`/`Failed —`/`Cancelled`), each with a comment so a future edit cannot "restore" the nicer character and re-break it. The other 99 occurrences are untouched and now simply render. **(c)** The settings footer hint moves into a pure `ui::settings_footer_hint()` and is elided with `fit_text` — newly necessary, since spelling the arrows out took the longest hint from 650 px to 760 px, exactly the text budget of a maximum-width panel and over it on any smaller window. Both symptoms confirmed fixed against the running app under Xvfb. No `.osv` change; `INDEX_VERSION` stays 12. 2067 tests / 0 failed; ASAN clean; Release, `--no-av` and clang legs green. → [details](docs/roadmap/phase-83-text-unicode.md) |
| 84 | Import perf, persisted gallery view, Home/End, crash-dump docs | ✅ | **(a) Import perf:** `ui::ArchiveReaderCache` (src/ui/archive_reader_cache.{h,cpp}) eliminates the O(n²) recursive-import cost of Phase 53/PR #124: a nested archive no longer opens a fresh reader for every extracted entry, instead reusing one reader per archive buffer via cache keyed on (data ptr, size). Root cause was the stateless recursive-hooks architecture — each `arc_list()` / `arc_extract()` call built a fresh `ArchiveReader`, so an N-entry solid archive decompressed O(N²) bytes total (full re-scan per entry). Fix: one ArchiveReader per buffer, retained across entry extractions via a forward stream cursor. Phase 84's `RecursiveHooks::archive_done` frame-pop hook drops the reader before its buffer dies. Structural observable: nested 7z → cache opens = 2 (outer + inner), vs old O(N+N²) for the same import. **(b) Persisted gallery view:** `ui::gallery_view_label/slug/from_slug/prev_gallery_view` (single table); platform::GalleryViewPref (gallery_view.conf, ThemePref pattern); L-key shows "View: <label>" in the grid footer + live-saves; App seeds at init and in promote_pending; F2 settings Appearance row "Default Gallery View" synced to session + live grid. **(c) Home/End:** Jump to first/last item in GalleryGrid (ScrollFollow::Center) + F1 help entry + NavModel clamp test. **(d) Windows crash dumps:** README section on WER LocalDumps registry setup and security caveats (crash dumps capture mlock'd frames during swap, mitigated by `prctl(PR_SET_DUMPABLE, 0)` on Linux). 2080 tests / 0 failed; ASAN clean. → [details](docs/roadmap/phase-84-import-perf-gallery-ux.md) |
| 85 | MPEG-PS playback, A/V seek sync, auto-play | ✅ | **(a) MPEG-PS (.mpg) support:** FFmpeg's `mpegps` demuxer defers video codec identification to the codec probe, which requires the **raw `mpegvideo` probe demuxer** (missing from our decode-only build, `CONFIG_MPEGVIDEO_DEMUXER 0`). Enabled in both `scripts/build_codecs.sh` and `scripts/build_ffmpeg_windows.sh`. The `PROBE_CAPS_GEN` bumps 1 → 2, re-offering the Phase 65 unlock-time migration which re-probes and heals existing Unknown-codec `.mpg` nodes without new code. **(b) A/V desync on seek:** The video worker skipped to the target, but audio played from the demuxer's keyframe (audibly pre-target sound). Fix: `media::audio_seek_skip` drops audio frames that end before the target; `VideoPlayback::pump_audio` skips them (never fed to SDL); the clock re-bases to the first kept frame's actual pts. Incidental hardening: `dup_video_sig` sws_scale overrun fixed with 4-element planes + padded stride. **(c) Auto-play (default ON):** New `platform::AutoplayPref` (config-dir `autoplay.conf`, missing/invalid → `true`), `media::autoplay_setting` (process global, UI-thread only), F2 Playback section (SettingsSection 5 → 6), and `VideoPlayback::set_paused(bool)` (public transport control). Viewer playback-build path calls `set_paused(false)` if auto-play enabled; behavior change — videos now start playing on open by default. 2098 tests / 0 failed; ASAN clean. → [details](docs/roadmap/phase-85-video-playback.md) |
| 86 | Range selection & scoped duplicate finder | ✅ | **(a) Shift+Space range selection:** every multi-select surface (gallery grid, favorites/tag screens, advanced-search Results focus) fills the span between the range anchor and the focused item — covering both "anchor then extend" and the literal "select two items, then fill between" (`SelectionModel::range_for` falls back to the previous toggled-on index when the focus sits on the anchor). New anchor-tracking in `ui::SelectionModel` (`select`, `select_range`, `range_for`; toggle-off demotes the anchor, `clear`/Ctrl+A never establish one); the grid's fill skips non-selectable tiles via `is_selectable` without moving the anchor. **(b) Duplicate-finder scope:** opening `Ctrl+D` from inside a non-root gallery offers a Scope row (Left/Right cycles) — *Whole vault* (historical), *This gallery only* (`collect_scan_items` gains a subtree scope parameter, proportionally cheaper scan), or *This gallery vs whole vault* (full-vault hash, then pure `scope_filter_groups` keeps only groups touching the browsed subtree — outside copies stay in their groups so either side is removable; component-boundary safe, `a` never matches `ab/x`). Scope shown in the chooser and review header via pure `dup_scope_label`; waves/marking/apply unchanged. 2120 tests / 0 failed. → [details](docs/roadmap/phase-86-range-select-dup-scope.md) |
| 87 | Migration leaves the grid holding freed node pointers (SEGV) | 🔜 | A finished `MigrationJob` leaves the active gallery grid with dangling `const IndexNode*` — its `compact()` rebuilds the index tree and frees the old one (`root_ = std::move(new_root)`), and `App::update` collected the outcome without ever re-listing the screen. The next render read a freed node's `name` and crashed (SIGSEGV in `byte_at()`, `_M_p == 0`), confirmed from the 2026-08-25 2nd-vault core. New pure `app::apply_migration_refresh()` re-lists + redraws the active screen right after `take_outcome()` (the same refresh the import drain already does), and the screen's `update()`/`render()` are paused while the job owns the vault exclusively (the `migration_job.h` contract the App was not honouring). No `.osv` change; `INDEX_VERSION` stays 12. 2131 tests / 0 failed; ASAN clean; no new TSan races. → [details](docs/roadmap/phase-87-migration-stale-pointers.md) |
| 88 | Vault files are not owner-only — 0644 on creation, silently truncated | 🔜 | Phase 4 of the break-in/hardening effort (`docs/break-in-effort.md`) established the threat model is **local** — an attacker who can read the user's home tree can read anything landing there with loose permissions. A `.osv` vault is the same class of secret as a keyfile (the point of the container is that a stolen *file* stays closed by the password/keyfile), yet **new vaults** were created with `fopen(path, "w+b")` → `0666 & ~umask` = **0644, group/other-readable**, while the keyfile that protects them gets explicit 0600 / current-user-only DACL — the more secret file had looser permissions than the less secret one. Worse, `"w+b"` **silently truncates an existing file**: creating a vault over an existing one destroyed it with no error (a data-loss path the exclusive-create keyfile already guards against). And **existing vaults** kept whatever permissions they had forever. Fixed with `platform::create_owner_only_file()` (atomic exclusive create, owner-only — `O_EXCL`+0600 on POSIX, `CREATE_NEW` + current-user-only DACL on Windows, the DACL factored out of `open_new_keyfile` into a shared `build_owner_only_acl`) and `platform::ensure_owner_only_file()` (best-effort tightening of an existing vault at open time, warn-once on failure — a vault on a share owned by someone else must not lock the user out). `Vault::create` now refuses an existing file with the new `AlreadyExists` result (unlock screen: "A vault already exists at that path.") instead of truncating; `Vault::open` tightens to owner-only so pre-existing vaults self-heal on the next unlock. No `.osv` change; `INDEX_VERSION` stays 12. 2134 tests / 0 failed (baseline 2131 + 3); ASAN clean. → [details](docs/roadmap/phase-88-vault-file-perms.md) |
| 89 | Decoded pixels were a plain swappable `std::vector` (break-in Phase 4 finding) | 🔜 | Phase 4 of the break-in/hardening effort (`docs/break-in-effort.md`) measured that the *decoded* image — the largest plaintext blob the app holds, 26–36 MB per photo — was a plain `std::vector<uint8_t>` (`image.h`): never page-locked, never explicitly wiped, so "no plaintext to disk" for it held only by the luck of a zram-only swap config, and the degrade-to-swappable path was invisible. `ImageData::pixels` is now a `crypto::SecureBytes` (move-only, mlock'd best-effort + `MADV_DONTDUMP` on Linux + `crypto_wipe` on destruction), exactly like the encrypted stored bytes; both draw on the same 256 MiB budget, so exhaustion degrades that one buffer to swappable with the existing warn-once log and a queryable `is_locked()` instead of silently living in swappable memory unmarked. `SecureBytes` gained the vector ergonomics the decode path needs — `operator[]`, `assign(span)`, `fill(n,v)` — all `[[nodiscard]]`, OOM returns `false` leaving the object empty, and every decode site (stb/WebP/HEIF/video poster) now handles that failure with `nullopt` where a plain vector would have thrown. No `.osv` change; `INDEX_VERSION` stays 12. 2138 tests / 0 failed (baseline 2134 + 4); ASAN clean; TSan clean (only the known local `radeonsi_drv_video.so` driver race, absent on CI runners). → [details](docs/roadmap/phase-89-pixels-secure-bytes.md) |
| 90 | Secure-memory state invisible + no system-config guidance (break-in Phase 4/6) | 🔜 | Two visibility/documentation gaps the break-in effort surfaced: **(a)** a GUI user never sees stderr, so the startup "secure-memory budget below 256 MiB" warning and every subsequent mlock failure (decoded data degrading to swappable memory) were invisible in-app. `draw_help_popup`'s Global group now carries a live status line — `Secure memory: 8 MiB page-lock budget active (best-effort)` / `… — some decoded data is swappable (mlock exhausted)` — backed by `platform::lockable_budget_bytes()` (soft `RLIMIT_MEMLOCK` / Windows minimum working-set, `SIZE_MAX` when unlimited), `crypto::mlock_failure_seen()` (the warn-once flag made a monotonic queryable atomic), and pure `ui::secure_mem_status_line(budget, degraded)` (empty-key `HelpEntry` renders without brackets); both wording paths verified in the running app under Xvfb (default budget, and `ulimit -l 0` for the degraded line). **(b)** the README's `RLIMIT_MEMLOCK` section didn't mention the app's own 256 MiB startup grow, the new F1 status line, or that mlock keeps data out of *swap* but not out of RAM (zram swap / true hibernate / Windows `hiberfil.sys`), and the Debugging section didn't state that Linux **Debug** builds keep core dumps by design — the exact vector of the Phase 1 break-in — so it now says to treat a Debug crash's core as vault-sensitive (`coredumpctl list` / `sudo rm …`) and prefer Release for a live vault. No `.osv` change; `INDEX_VERSION` stays 12. 2144 tests / 0 failed (baseline 2138 + 6); ASAN clean. → [details](docs/roadmap/phase-90-secure-mem-visibility.md) |

---

## Container format spec (reference)

Reproduced from `mem:vault_format` (Serena) for quick access during vault
implementation work.

```
Offset  Size  Description
──────  ────  ───────────────────────────────────────────────────────────────
0       8     magic: "OSVAULT\0"
8       2     version (u16, currently 1)
10      2     header_size (u16, total header length in bytes)
12      4     flags (u32, reserved)

16      1     kdf_algo  (0 = Argon2id)
17      4     t_cost    (u32, Argon2id time cost)
21      4     m_cost_kib (u32, Argon2id memory cost in KiB)
25      4     parallelism (u32)
29      16    salt      (u8[16], random)
45      1     keyfile_required (u8, 0 or 1)

46      24    master_key_nonce (u8[24])
70      32    wrapped_master_key (u8[32], XChaCha20-Poly1305 ciphertext)
102     16    master_key_tag (u8[16], Poly1305 tag)

118     8     slot_a_offset (u64)
126     8     slot_a_length (u64)
134     24    slot_a_nonce  (u8[24])
166     8     slot_b_offset (u64)
174     8     slot_b_length (u64)
182     24    slot_b_nonce  (u8[24])
206     1     active_slot   (u8, 0 = A, 1 = B)

207     N     reserved padding (zeroes, up to header_size)
```

**Data region** starts at `header_size`. Each encrypted chunk is laid out as:
```
  nonce[24] | ciphertext[plaintext_len] | tag[16]
```

**Framed vaults (header flags bit 0, Phase 26):** the AEAD plaintext of every
chunk AND of the sealed index blob is a chunk_codec frame:
```
  method u8 (0 = raw, 1 = deflate)
  if raw:     payload bytes
  if deflate: orig_len u64 LE | zlib-wrapped deflate stream
```
Flag clear (legacy): the plaintext is the payload verbatim, read and appended that
way forever.

**Index blob** (binary serialised; `INDEX_VERSION = 6`):
```
  version    u8
  root       node              (the tree, recursive — see below)
  saved_searches (v5+):                (Phase 18; omitted in v1–v4 blobs → empty)
    count    u16
    entries  { name_len u16; name u8[name_len];
               query_len u32; query u8[query_len] } [count]
```

**Index tree node** (binary serialised):
```
  node_type  u8  (0 = gallery, 1 = image, 2 = video)
  name_len   u16
  name_len   u16
  name       u8[name_len]  (UTF-8)

  tag_count  u16                     (Phase 12; v2+. Omitted entirely in v1 blobs.)
  tags       { tag_len u16; tag u8[tag_len] (UTF-8) } [tag_count]

  favorite   u8                      (Phase 13; v3+. Omitted in v1/v2 blobs → 0.)

  if gallery:
    sort_key     u8                 (Phase 37; v6+. Manual/NameAsc/NameDesc/
                                      DateAsc/DateDesc/SizeAsc/SizeDesc.
                                      Omitted in v1–v5 blobs → Manual.)
    child_count  u32
    children     node[child_count]  (recursive)

  if image:
    format       u8  (0=JPEG, 1=PNG, 2=GIF, 3=BMP, 4=TGA, 5=HDR, 6=WebP, 7=HEIC, 8=AVIF)
    width        u32
    height       u32
    orig_size    u64  (plaintext bytes)
    created_ts   u64  (Unix timestamp, seconds)
    data_offset  u64
    data_length  u64
    thumb_offset u64
    thumb_length u64
```

> **Format extensions (Phases 12–18).** The index serialisation is
> versioned; each of these bumps `INDEX_VERSION` and reads older versions with
> the new fields defaulted, so existing vaults keep opening cleanly:
> - **Phase 12 (Tags):** ✅ shipped — a tag list (`u16 count` + length-prefixed
>   UTF-8) on **both** gallery and image nodes, written after `name`
>   (`INDEX_VERSION = 2`; v1 blobs read with empty tags). Gallery tags cascade to
>   descendants at read time (effective tags = own ∪ ancestors'); they are not
>   copied onto children.
> - **Phase 13 (Favorites):** ✅ shipped — a `favorite u8` flag on both node types,
>   written after the tag block (`INDEX_VERSION = 3`; v1/v2 blobs read as
>   not-favorited).
> - **Phases 15–16 (Video):** a video node kind (a `media_kind` discriminator)
>   and new `format` codes appended after `8=AVIF` (e.g. `9=MP4/H.264`); video
>   nodes reuse the same `data_*`/`thumb_*` layout (thumb = poster frame), with
>   the container stored across multiple encrypted chunks.
> - **Phase 18 (Advanced search):** ✅ shipped — a **vault-global saved-searches
>   block** serialised after the tree root (`u16 count` + per-entry `{ name,
>   serialised query }`, the query an opaque `ui::AdvancedQuery` blob), bumping
>   `INDEX_VERSION` to **5**; pre-v5 blobs read with an empty saved-searches list.
>   The block is not part of any node — it is vault-level metadata, persisted via
>   the same crash-safe double-buffer index swap and bounded against the fuzz suite.
> - **Phase 37 (Persisted sort order):** ✅ shipped — a `sort_key u8` on every
>   Gallery node (`INDEX_VERSION = 6`; v1–v5 blobs read as `Manual` — no visible
>   change until a user opts in via `Shift+S`). An out-of-range byte is
>   rejected on deserialise, not clamped.
> - **Phase 47 (Animated GIFs):** ✅ shipped — an `animated u8` flag on every
>   Image node, written after `thumb_length` (`INDEX_VERSION = 7`; v1–v6 blobs
>   read as not animated and are healed lazily on first view). A byte other
>   than 0/1 is rejected on deserialise, not clamped.
> - **Phase 49 (Tag chips & per-vault settings):** ✅ shipped — a **vault-global
>   settings block** serialised after the saved-searches block (`default_sort u8`,
>   `tiles_show_tags u8`, then `u16 count` + per-entry `{ name, swatch u8 }` tag
>   categories), bumping `INDEX_VERSION` to **8**; pre-v8 blobs read with the
>   default sort `Insertion`, tile tags on, and the built-in category seed. The
>   same bump re-reads a Gallery's `sort_key` byte 0 as `Default` ("follow the
>   vault default") and adds `7 = Insertion` for raw import order, so existing
>   galleries adopt the vault default with no migration. Out-of-range
>   swatch/sort/flag bytes are rejected on deserialise, not clamped.
> - **Phase 51 (Tag metadata, folder import & organisation polish):** ✅ shipped — a
>   **tag-descriptions sub-block** serialised after the Phase 49 settings block, each
>   entry a length-prefixed name + description pair, bumping `INDEX_VERSION` to **9**;
>   pre-v9 blobs read with empty descriptions. An out-of-range name/description length
>   is rejected on deserialise, not clamped.
> - **Phase 65 (Blocking migration):** ✅ shipped — a vault-global **migration
>   watermark** appended to the settings block (`migrated_index_version u8`,
>   `migrated_probe_caps u16`), bumping `INDEX_VERSION` to **10**; pre-v10 blobs
>   read `0/0` and are therefore treated as never migrated. A watermark claiming
>   a future index version is rejected on deserialise, not clamped. The lazy
>   in-browse repairs (`ui/video_repair.*`, `ui/anim_repair.*`) are removed.
> - **Phase 73 (Tag category templates & non-blocking tagging):** ✅ shipped — 
>   category template fields appended to each category entry, and a vault-global 
>   **tag-field-values block** appended after the migration watermark (`tag`, `field`, 
>   `value` entries; single value per (tag, field); matched case-insensitively), 
>   bumping `INDEX_VERSION` to **11**; pre-v11 blobs read with no templates and no 
>   values. An out-of-range field-count or entry-count is rejected on deserialise, 
>   not clamped.
> - **Phase 75 (Pull transfers, tag persistence, 512px thumbnails):** ✅ shipped — 
>   thumbnail-budget watermark appended to the end of the settings block 
>   (`migrated_thumb_side` u16 — the stored-thumb long side this vault's 
>   thumbnails/posters were last generated at; 0 = legacy), bumping 
>   `INDEX_VERSION` to **12**; pre-v12 blobs read `0` and are *offered* the 
>   one-time thumbnail regeneration at the next unlock (the blocking 
>   `MigrationJob`; cancel keeps committed work but leaves the watermark 
>   unstamped, so it is re-offered). `Vault::create` stamps 
>   `migrated_thumb_side = 512` and `migrated_index_version` so fresh vaults 
>   owe nothing. Cross-vault transfers lower the destination's watermark to 
>   the source's when the source is behind (mirrors the Phase 65 rule). The 
>   field is version-gated on read; every u16 is a legal value (the 
>   reject-not-clamp rule applies to `migrated_index_version`, not here).
